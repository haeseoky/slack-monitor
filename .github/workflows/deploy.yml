name: Deploy to Oracle Server

on:
  push:
    branches:
      - main
    paths-ignore:
      - '**.md'
      - '.gitignore'
      - 'LICENSE'
  workflow_dispatch:  # 수동 실행 가능

jobs:
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest

    # Secrets가 설정되어 있을 때만 실행
    if: ${{ secrets.ORACLE_HOST != '' }}

    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔍 Check SSH Connection
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_USERNAME }}
          key: ${{ secrets.ORACLE_SSH_KEY }}
          port: ${{ secrets.ORACLE_PORT || '22' }}
          script: |
            echo "✅ SSH 연결 성공"
            echo "현재 경로: $(pwd)"
            echo "사용자: $(whoami)"

      - name: 🚀 Deploy to Oracle Server
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_USERNAME }}
          key: ${{ secrets.ORACLE_SSH_KEY }}
          port: ${{ secrets.ORACLE_PORT || '22' }}
          script: |
            DEPLOY_PATH="${{ secrets.DEPLOY_PATH }}"
            if [ -z "$DEPLOY_PATH" ]; then
              DEPLOY_PATH="$HOME/slack-monitor"
            fi

            echo "📂 배포 경로: $DEPLOY_PATH"

            # 디렉토리 존재 확인
            if [ ! -d "$DEPLOY_PATH" ]; then
              echo "❌ 배포 경로가 존재하지 않습니다: $DEPLOY_PATH"
              echo "💡 서버에서 먼저 프로젝트를 클론하세요:"
              echo "   git clone https://github.com/${{ github.repository }}.git $DEPLOY_PATH"
              exit 1
            fi

            cd "$DEPLOY_PATH"

            # deploy.sh 실행 권한 확인
            if [ ! -x "./deploy.sh" ]; then
              echo "🔧 deploy.sh에 실행 권한 부여"
              chmod +x ./deploy.sh
            fi

            # 배포 실행
            ./deploy.sh

      - name: ✅ Deployment Status
        if: success()
        run: |
          echo "🎉 배포 완료!"
          echo "Repository: ${{ github.repository }}"
          echo "Commit: ${{ github.sha }}"
          echo "Author: ${{ github.actor }}"

      - name: ❌ Deployment Failed
        if: failure()
        run: |
          echo "💥 배포 실패!"
          echo "GitHub Secrets 설정을 확인하세요:"
          echo "  - ORACLE_HOST: 서버 IP 또는 도메인"
          echo "  - ORACLE_USERNAME: SSH 사용자명"
          echo "  - ORACLE_SSH_KEY: SSH 개인키 (private key)"
          echo "  - ORACLE_PORT: SSH 포트 (선택, 기본값: 22)"
          echo "  - DEPLOY_PATH: 배포 경로 (선택, 기본값: ~/slack-monitor)"
          echo ""
          echo "자세한 설정 방법: README.md 참고"
